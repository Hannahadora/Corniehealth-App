export interface Codeable {
  code: string;
  display: string;
}

export interface Timeable {
  age: string;
  startDate: string;
  startTime: string;
  endDate: string;
  endTime: string;
  date: string;
  time: string;
}

export interface Measurable {
  unit: string;
  min: string;
  max: string;
  string: string;
}
